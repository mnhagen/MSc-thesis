\chapter{\label{cha:background}Background \& Theory}

This chapter introduces the theoretical background needed for the
remainder of the thesis. It first places operator learning in context
with more classical learning setups and then discusses the neural
operator architectures that are central to this work.

SOMEWHERE IN HERE: explain bandlimits/nyquist frequency and how this relates to the resolution of the data and the model's ability to learn the underlying operator. This is a key concept that motivates the CNO architecture and its stability across resolutions, as well as the challenges faced by FNO when dealing with high-frequency information that may not be well-resolved in the training data.

\section{From standard machine learning to Operator Learning}

This section introduces operator learning as a mapping between
functions, rather than the more familiar vector-to-vector setting. It
should explain why this perspective is useful for PDE-governed physical
systems and introduce the CDE concept that motivates resolution-aware
learning.

Introduce the continuous-domain view and connect it to the notion of
discretization invariance used throughout the thesis.

\subsection{Discretization Invariance}

\section{Fourier Neural Operator}

This section should explain the FNO architecture and the spectral
convolution mechanism on which it is based.

\subsection{Architecture}

Describe the lifting layer, Fourier layers, spectral convolution,
non-linear transformations, and projection back to the target field.

\subsection{Strengths}

Discuss the global receptive field, computational efficiency through
FFT-based convolutions, and the suitability of FNO for smooth
solution operators.

\subsection{Weaknesses}

Discuss spectral truncation, sensitivity to resolution changes, and
the reliance on regular grids.

\section{Convolutional Neural Operator}

This section should explain the CNO architecture and the inductive
biases that differentiate it from FNO.

\subsection{Architecture}

Describe the encoder-decoder style structure, convolutional blocks, and
the way multi-scale processing is used.

\subsection{Bandlimited assumption}

Explain the bandlimited assumption and why it can stabilize learning and
support more consistent behavior across discretizations. Discuss how this means explicitly discarding high-frequency information, where the FNO keeps it (but does not know what to do with it).

\subsection{Local versus global inductive bias}

Compare the locality of convolutional operators with the global spectral
bias of FNO.

\section{Mesh-free Operator Learning and geo-FNO}

This section motivates operator learning on irregular domains and
introduces geo-FNO as the architecture used for this purpose.

\subsection{Motivation for irregular domains}

Explain why many scientific and engineering problems do not naturally
live on uniform Cartesian grids and why this matters for neural
operators.

\subsection{geo-FNO}

Describe the geo-FNO approach and how it extends Fourier-based operator
learning to non-uniform meshes.

explain the architecture itself and that it is a generalization of FNO that collapses back to FNO when the input points are on a regular grid. Also mention that you can just interpolate the input data to a regular grid and use FNO, but this is not ideal because it can introduce interpolation errors and does not leverage the potential advantages of learning directly on the original point cloud. cite the geo fno paper that tested this and found lower loss when learning directly on the point cloud with geo-FNO compared to interpolating to a regular grid and using FNO.

\section{Discretization invariance on mesh-free grids}

More philosophical discussion of what discretization invariance means when we no longer have a regular grid and how this relates to the notion of a continuous-domain operator. 

something like: 

In this thesis, discretization invariance is interpreted as the ability of a model trained on data at one uniform resolution to make accurate predictions on the same underlying problem at different uniform resolutions. This notion does not transfer directly to non-uniform point clouds, where there is no single grid resolution. For non-uniform data, the relevant question is instead whether the sampled points sufficiently resolve the solution features needed for prediction.

talk about this more in method/results chapter when we discuss loss as function of resolution.


FINAL VERSION:

\section{Operator Learning}

Operator learning refers to the use of neural networks to approximate mappings between function spaces, rather than between finite-dimensional vectors as is done for classical neural network approaches \cite{neural-operators}. This setting arises naturally in scientific computing, where many problems can be formulated in terms of operators acting on functions.

A particularly important example is the solution operator of a parametric partial differential equation (PDE). Consider a PDE of the form:
\begin{equation}
\mathcal{N}(a, u)(x) = f(x), \quad x \in \Omega, \qquad u(x) = 0, \quad x \in \partial \Omega,
\end{equation}
where $\Omega \subset \mathbb{R}^d$ is a bounded domain, $\mathcal{N}$ is a differential operator, $a$ is an input function, $f$ is an external term like a forcing term, and $u$ is the solution.

The corresponding solution operator is defined as:
\begin{equation}
\mathcal{G}(a) = u,
\end{equation}
which maps an input function $a$ to the solution $u$. The goal of operator learning is to approximate this mapping directly using surrogate modeling. 

Let $\mathcal{A}$ and $\mathcal{U}$ denote appropriate function spaces for the input and output, respectively. Given a dataset of input-output pairs:
\begin{equation}
\{(a_j, u_j)\}_{j=1}^{N_{\text{data}}}, \quad u_j = \mathcal{G}(a_j),
\end{equation}
where the inputs $a_j$ are sampled from a distribution $\mu$ over $\mathcal{A}$, the objective is to learn a parametric approximation $\mathcal{G}_\theta : \mathcal{A} \to \mathcal{U}$ by minimizing the expected error
\begin{equation}
\mathbb{E}_{a \sim \mu} \left[ \| \mathcal{G}(a) - \mathcal{G}_\theta(a) \|_{\mathcal{U}}^2 \right],
\end{equation}
which in practice is approximated by the empirical loss over the dataset.

A key distinction from standard learning problems is that the objects of interest are functions rather than finite-dimensional vectors, even though in practice they are only observed through discrete samples. Consequently, the learning problem is formulated at the level of a continuous operator, while training is carried out on its discretized representations. This makes it essential to understand what information about a function is preserved under discretization. 

\subsection{Bandlimitedness and Continuous--Discrete Equivalence}\label{sec:bandlimitedness}

A central question in operator learning is what information about a continuous function is preserved when it is represented through discrete samples. Sampling theory provides a framework for answering this question, by characterizing when a function can be uniquely reconstructed from its values on a discrete grid.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/aliasing.png}
    \caption{\label{fig:aliasing} an illustration of aliasing. The blue curve represents a high-frequency signal, while the red curve is a lower-frequency function that passes through the same sampled points (black dots). Since both functions agree at the sampling locations, the discrete representation cannot distinguish between them.}
\end{figure}

Let $f \in L^2(\mathbb{R})$ be a square-integrable function with Fourier transform $\hat{f}$. The function is said to be \textit{bandlimited} with bandlimit $\Omega > 0$ if its Fourier transform is supported only on a finite interval:
\begin{equation}
\mathrm{supp}(\hat{f}) \subset [-\Omega, \Omega].
\end{equation}

The Whittaker-Shannon sampling theorem states that a bandlimited function can be exactly reconstructed from its values on a uniform grid, provided the sampling rate is sufficiently high \cite{signals-and-systems}. Specifically, if the function is sampled at points $\{f(nT)\}_{n \in \mathbb{Z}}$, then exact reconstruction is possible if
\begin{equation}
\frac{1}{T} \geq 2\Omega,
\end{equation}
where $1/T$ is the sampling frequency and $2\Omega$ is called the Nyquist rate.

Under this condition, the function can be reconstructed via the interpolation formula
\begin{equation}
f(x) = \sum_{n \in \mathbb{Z}} f(nT)\,\mathrm{sinc}\left(\frac{x - nT}{T}\right),
\end{equation}
where $\mathrm{sinc}(x) = \frac{\sin(\pi x)}{\pi x}$.

This result establishes a form of continuous-discrete equivalence: if a function is bandlimited and sampled above the Nyquist rate, then its discrete samples contain all the information required to reconstruct the original function. In this case, the discretization is effectively lossless.

However, if the sampling rate is below the Nyquist rate, this equivalence breaks down. In this case, different continuous functions can produce exactly the same set of discrete samples, meaning that the original function can no longer be uniquely reconstructed from the sampled data.

This phenomenon is known as aliasing. Intuitively, aliasing occurs when oscillations in the function are too rapid relative to the spacing between sample points. If the grid is too coarse, the samples cannot resolve the true high-frequency behavior, and the function appears to vary more slowly than it actually does. 

Figure~\ref{fig:aliasing} illustrates this effect. The blue curve represents a high-frequency signal, while the red curve is a much lower-frequency function passing through the same sampled points. Since both functions agree exactly at the sampling locations, the discrete representation cannot distinguish between them. From the perspective of the sampled data, the high-frequency signal is therefore indistinguishable from its lower-frequency alias.

From a Fourier perspective, a sampled grid can only represent frequencies up to the Nyquist limit
\begin{equation}
\omega_N = \frac{\pi}{T},
\end{equation}
where $T$ is the spacing between sample points. Frequencies satisfying
\begin{equation}
|\omega| \leq \omega_N
\end{equation}
can be represented uniquely, while frequencies above this limit cannot.

Consider a sinusoidal component
\begin{equation}
f(x) = \sin(\omega x).
\end{equation}
Sampling this function at points $x_n = nT$ gives
\begin{equation}
f(nT) = \sin(\omega nT).
\end{equation}

If $\omega > \omega_N$, there exists a lower frequency $\omega_{\mathrm{alias}} \leq \omega_N$ such that
\begin{equation}
\sin(\omega nT) = \sin(\omega_{\mathrm{alias}} nT)
\end{equation}
for all sample points $n$. A common expression for this aliased frequency is
\begin{equation}
\omega_{\mathrm{alias}} = |\omega - 2k\omega_N|,
\end{equation}
for some integer $k$ chosen so that
\begin{equation}
0 \leq \omega_{\mathrm{alias}} \leq \omega_N.
\end{equation}

This shows that frequencies above the Nyquist limit do not simply disappear. Instead, they are systematically and deterministically mapped onto lower frequencies that the grid can represent. Aliasing is therefore not random noise, but a predictable distortion of the frequency content.

The consequence is that information above the Nyquist limit is irreversibly lost. Once aliasing has occurred, the sampled data contains a distorted representation of the original function, where unresolved high-frequency behavior is interpreted as physically different low-frequency structure. No learning algorithm can reconstruct frequency content that is absent from the discretized data, regardless of model complexity or architecture \cite{bartolucci-discretization-invariance}.


\subsection{Discretization Invariance}

The discussion in Section \ref{sec:bandlimitedness} shows that learning from discretized data is fundamentally constrained by the information preserved under sampling. Even if a model is intended to approximate a continuous operator, it is trained and evaluated on finite representations of functions. This raises the question of whether the learned model depends only on the underlying function, or also on the particular discretization used to represent it.

This property is commonly referred to as discretization invariance. Informally, a discretization-invariant model should produce consistent predictions when the same underlying function is represented on different grids, provided those discretizations sufficiently resolve the relevant features of the problem.

In practice, discretization invariance is most naturally interpreted for functions represented on uniform Cartesian grids, where resolution can be clearly defined through grid spacing. In this setting, it refers to the ability of a model trained at one resolution to make accurate predictions at other resolutions of the same underlying problem.

A widely used characterization of neural operators is given by Kovachki et al. \cite{neural-operators}, who state that a model should satisfy three key properties:

\begin{enumerate}
    \item \textbf{Discretization flexibility:} the model can accept inputs defined on arbitrary discretizations of the domain.
    
    \item \textbf{Pointwise evaluation:} the model can be evaluated at arbitrary output locations.
    
    \item \textbf{Continuum consistency:} as the discretization is refined, the learned operator converges to the underlying continuous operator being approximated.
\end{enumerate}

These properties formalize the idea that a neural operator should approximate a mapping between functions, rather than a mapping between specific finite-dimensional representations. In particular, the learned operator should depend on the underlying continuous object and not on the resolution, ordering, or structure of the grid used to represent it.

Since training data is always discretized, a model is never directly exposed to the continuous operator itself, but only to finite samples of its input and output functions. If refining the discretization changes the behavior of the model rather than improving the approximation of the same underlying mapping, the model becomes tied to the representation used during training rather than to the operator itself. As a result, performance may degrade significantly when evaluated on different resolutions or different discretizations of the same problem. This phenomenon is commonly referred to as \textit{discretization mismatch} \cite{gao_discretization_2023}.

Discretization invariance is therefore a central criterion for evaluating neural operator architectures. In this thesis, it is used both as a theoretical concept and as an empirical measure for comparing models across changes in resolution.

\section{Fourier neural operator}

use the section already written but not currently accessible (mr gpt). to this section: add something about how global mixing/keeping global information is useful for many physical systems (cite source)

\section{Convolutional neural operator}

make it in the style/tone/length/structure of the FNO section. prompt:

next is the convolutional neural operator. the CNO can be seen as an extension of the U-net neural network that have proven to be effective in fields like image processing (cite source). 

insert figure of the CNO architecture \label{fig:cno-architecture}

we start by introducing the U-net. the U-net is a convolutional neural network, meaning it uses a kernel to apply convolutions. when interpreting this through the lense of physical systems, this corresponds to local mixing, which is beneficial for many physical systems exhibiting strong locality like X and Y (cite source). (maybe mention that this contrasts with the global bias of the FNO? this somewhat breaks the flow and does not feel like it fits in the CNO introduction section. I think maybe this is best saved for the discussion/comparison section) One limitation of convolutional neural networks when applied to physical systems is that the physical meaning of a certain kernel size depends on the resolution, as it will look at different spatial scales (include a figure here? explain in more detail?). CNOs borrow from U-nets and address this issue by using a multi-scale architecture, where the convolutional kernels are applied at multiple resolutions; see figure \ref{fig:cno-architecture}. The input is first fed through a lifting layer acting on the feature space (read thoughts at the end about the structure here). the input then passes through a convolutional block, consisting of a convolutional kernel application followed by a nonlinearity. as the representation descends on the encoder side (rephrase/explain this more), the spatial resolution is decreased using interpolation-based downsampling while the feature space "resolution" is increased. the input is again passed through a convolutional block, but as the kernel size is the same across all blocks, a different spatial scale is captured. on the decoder side, the coarse resolution is upsampled before another convolutional block is applied. to access higher frequency content that was lost to the downsampling, skip connections are in place so that the network can "see" what the input looked like on the same resolution on the encoder side. (maybe mention projection before final prediction is made, not sure if its necessary)

While the U-net structure succeeds at capturing multiple physical scales, it is not a discretization invariant architecture. The CNO makes several modifications to the U-net structure aimed at making the U-net discretization invariant/robust. First and most important is resampling onto a fixed internal grid/resolution. by doing this, the CNO always treats the same resolution (connect to bandlimitedness; we are implicitly assuming that the chosen resolution/the bandlimit it imposes captures sufficient detail to let the model learn the solution operator). when the resampling is downsampling, higher frequency content is explicitly truncated, which helps to stabilize the model. even if the CNO could access higher frequncy content at inference, if it has not seen it during training, it would not know what to do with it. explicitly removing the higher frequency content helps satabilize the model performance when testing on higher resolutions. at lower resolutions, upsampling should be equivalent to zero padding in fourier space (maybe unnecessary reference to fourier space) and should introduce minimal errors/discrepancies. regardless, interpolation errors are not unheard of. (decide whether to include an interpolation error section; could show the fourier power spectrum of navier stokes before and after resampling round trip, which shows that the real frequencies are retained, but higher frequency, unphysical/spurious content is generated by the round trip). the next modification of the CNO compared to the U-net is that the simple pointwise nonlinearity is superceded by a upsample-nonlinearity-downsample (UND) block. this change is made as the pointwise nonlinearity alone is not discretization invariant. The application of the nonlinearity creates higher frequency content. consider ... (mathematical proof of this fact). when the generated frequencies have frequencies above nyquist for a given resolution, they will fold down and contaminate the resolvable modes. As outlined in section \ref{sec:bandlimitedness}, the aliasing pattern is deterministic and dependent on the resolution, which violates discretization invariance (this doesn't make any sense. for the CNO, the UND block is applied only after the resampling, so the aliasing pattern is always the same. why exactly does the UND block lead to a more discretization invariant model? why is it included, what is it intended to do, and why does it help?). to solve this, the CNO uses the aforementioned UND block. before the nonlinearity is applied, the data/input is first upsampled to a higher resolution, which is equivalent to zero padding in fourier space. when the nonlinearity is then applied, the higher frequency content has space to exist without folding into lower frequencies. after the nonlinearity is applied, the data is downsampled back to the original resolution using a X filter (I do not remember the name, but it is not a true low pass filter. it is an approximation. this is important to cover in some depth, as this is why the CNO without the UND block outperforms the standard CNO at high resolutions of navier stokes, so i need to be able to refer back to it here when i cover it in discussion), which is equivalent to truncating the higher frequency content. by doing this, the CNO can safely apply nonlinearities without generating spurious low-frequency content that would violate discretization invariance. There are some other minor tweaks, but they are not theoretically important. just make sure to not make it sound like resampling onto a fixed internal grid + UND are the only changes, they are just the main ones

for strengths, the CNO is a robust model. the initial resampling ensures stable performance across resolutions,given that the training resolution is sufficient to capture the relevant features of the problem. convolutions are useful for many physical problems that exhibit strong local bias like X and Y (cite source) (I see now that this is very similar to something i wrote earlier in the CNO section. I think it makes sense to mention at depth here; remove or shorten it where i wrote it before). the multi-scale architecture allows it to capture a wide range of physical scales, which is important for many physical problems. it is especially useful for problems like Navier stokes that see energy transfer across length scales (cite source). the UND block also ensures that it is a true neural operator, as it can apply nonlinearities without generating spurious low-frequency content that would violate discretization invariance \cite{bartolucci-discretization-invariance}. others?

for weaknesses, the CNO is quite computationally expensive due to the use of convolutional layers and the multi-scale architecture. the changes made compared to a U-net also add to the computational cost. it also has more hyperparameters to tune, which can make training more difficult (is this true?). the resampling step can introduce interpolation errors, which can degrade performance if not done carefully. Others?

one problem i see with this structure is that i explain a U-net in some depth, but i do not make it clear whether this is about the u-net or the CNO. i later explain in which way the CNO builds on and "improves" the U-net, and only then do i explain that the CNO first resamples the input onto a fixed internal grid. this was not mentioned in my explanation of the u-net structure, which could confuse the reader. the only reason i dont make it crystal clear that I am talking about u-nets when i first explain this is that i am referring to a figure that shows the CNO structure. it is close enough to the U-net that keeping both seems unnecessary. 


\section{Mesh-free Operator Learning and geo-FNO}

short intro section? or straight to irregular domains?

\subsection{Motivation for irregular domains}

While successful, the operators introduced in section \ref{fno-section} and \ref{cno-section} run into the issue of requiring the input data to be structured on a uniform, rectangular, Cartesian grid. This is often a very inefficient way of storing the data. a dataset with high variance in one area and low variance in another can much more effectively capture the dataset with varying sampling rates (cite source). other datasets may simply be on a geometry that does not lend itself to a Cartesian representation (cite source). While one could always interpolate a dataset of an arbitrary structure onto a Cartesian grid and feed that to uniform grid neural operators, this has been shown to incur signicant interpolation errors, hampering the performance of these models significantly (cite source; i think there was a good one in the introduction of the geo fno paper). This motivates extending neural operators to non-uniform meshes/point clouds. other things to bring up here?

\subsection{geo-FNO}

The geo-FNO is one of the most significant non-uniform neural operators. The architecture directly builds upon the original FNO, effectively being a generalization of it (cite source). The geo-fno, short for geometry-aware FNO, consists of a coordinate transform in the form of a light MLP in front of an FNO as described in section \ref{fno-section}. the coordinate transform, referred to as IPHI (use inverse of greek letter phi), is trained jointly with the FNO and thus learns a transform that minimized the loss. the grid following the transformation is referred to as the computational grid. this non uniform grid is the first input to the FNO, with the FFT replaced with a non uniform DFT. after the first Fourier layer, when the inverse fourier transform is taken, we return to physical space on a uniform grid, which is used throughout the FNO until the final output. this compromise delivers better performance than the interpolation onto a uniform grid outlined in section \ref{motivation for irregular domains}, while maintaining the efficiency of fast fourier transforms within the FNO. 

The geo-FNO has the advantage that it can be evaluated at arbritrary positions. This is because the final FNO layer has a stage where it has the fourier coefficients of its solution prediction, which is defined everywhere in physical space. The geo-FNO allows for a list of points to be passed, and prediction can be made on said points despite these points not being seen during training. The geo-FNO therefore satisfies two of the points outlined in section \ref{discretization invariance}, as it can take input and output at arbitrary positions.

iphi is a coordinate transform that takes the form: iphi(x, a) = x + f(x, a) where x is the set of input points and a is an optional vector of geometry parameters. f(x,a) is initialized as identity, stabilizing training. iphi is trained jointly with the FNO, meaning that the coordinate transform learns to minimize the loss while adapting to the geometry of the input data. This could mean mapping to a distribution where energy is shifted away from the higher modes that the FNO cannot resolve and into lower modes that are utilized. note that iphi does not map onto a discrete grid; this is a continuous to continuous mapping. the discrete fourier transform has its problems, as the resulting Fourier basis is in general not orthogonal (include proof of this; show why the basis is orthogonal for a uniform grid, and explain which step does not hold for non uniform grids). this could cause issues because ... (i am actually not sure what the consequences could be; help me with this). the vector a lets the model access information about geometric parameters that could differ between samples. an example of these is the aerospace airfoil thingy i need to find a source for, where something like 20-30 different parameters together span a continuous space. by feeding iphi these parameters, the mapping can be tailored to the exact geometric setup, letting the geo-FNO make a wider range of predictions more efficiently. (somewhere in the iphi section: include a couple of lines + source showing that such a coordinate transform can be effective. ex. chebyshev basis from original geo fno paper)

are there other things to mention about the geo-FNO? it is really just a coordinate transform wrapper around an FNO already described in the FNO section. the FNO itself is unchanged except for the initial fourier transform and final inverse transform being replaced by non-uniform approximations. let me know if i am missing something or if this is enough

\section{Discretization invariance on mesh-free grids}

More philosophical discussion of what discretization invariance means when we no longer have a regular grid and how this relates to the notion of a continuous-domain operator. 

In this thesis, discretization invariance is interpreted as the ability of a model trained on data at one uniform resolution to make accurate predictions on the same underlying problem at different uniform resolutions. This notion does not transfer directly to non-uniform point clouds, where there is no single grid resolution. For non-uniform data, the relevant question is instead whether the sampled points sufficiently resolve the solution features needed for prediction. This is more a question of where those points are instead of simply how many there are.

help me with this section. what i really want to say is that discretization invariance is an easy concept for uniform grids, as an increase in resolution is well defined. on non uniform grids/point clouds, this is more complicated. the sheer number of points is not nearly as important as their distribution. i want to explain that because of this increase in complexity, no resolution experiments were conducted for non uniform grids. i want to have more of an explanation than that, however, and explain how my results and the fact that the models produces proper predictions is indicative of ... the section above i think is a good starting point

talk about this more in method/results chapter when we discuss loss as function of resolution.




